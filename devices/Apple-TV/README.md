# Apple TV

The first-generation Apple TV used an Intel Pentium-M-class x86 CPU. Later generations moved through ARMv7 A-series SoCs and then AArch64 A-series SoCs. tvOS arm64 should follow the same AArch64 selector with platform-specific ABI/linking metadata.
