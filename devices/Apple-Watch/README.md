# Apple Watch

Keep two CPU ABI targets: historical `armv7k`, and `arm64_32` (AArch64 instructions with a 32-bit pointer/data model). Apple Watch is one of the Apple products that genuinely adds an unusual ABI shape.
