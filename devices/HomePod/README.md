# HomePod

HomePod uses Apple ARM silicon, but Apple does not expose it as a general third-party native application deployment target in the same sense as iOS/macOS/tvOS/watchOS/visionOS. Record the hardware mapping; do not make it a required compiler target.
