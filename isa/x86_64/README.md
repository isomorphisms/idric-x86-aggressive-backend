# x86_64 / Intel 64

Intel Mac ISA. Apple contributes ABI/Mach-O constraints, not private x86 opcodes. This branch keeps a plain mnemonic catalog and treats CPU-generation SIMD extensions as capabilities.
