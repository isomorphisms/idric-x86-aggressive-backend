# arm64_32 (watchOS)

`arm64_32` uses AArch64 instructions with a 32-bit pointer/data model. It is an ABI target, not a distinct machine instruction set. Instruction selection follows AArch64 while pointer width, layout, calling convention and Mach-O metadata follow watchOS arm64_32.
