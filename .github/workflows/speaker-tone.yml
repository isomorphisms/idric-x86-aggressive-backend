name: x86-64 speaker tone oracle

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  speaker-tone:
    runs-on: ubuntu-latest
    steps:
      - name: Check out x86 backend
        uses: actions/checkout@v4

      - name: Build and inspect native speaker-tone oracle
        run: sh examples/speaker-tone/check.sh
