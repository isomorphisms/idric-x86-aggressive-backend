# Branches and calls

Model conditional branch, direct branch/call, indirect branch/call, return, compare and test separately. Apple ABI rules determine link-register/stack/unwind behavior; Mach-O relocation rules determine how external targets are represented.
