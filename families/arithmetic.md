# Arithmetic

Keep integer add/subtract/multiply/divide, flag-setting forms, widening/narrowing forms, and floating-point arithmetic separate in the IR. Apple does not change their mathematical semantics; the target chooses ISA encodings and ABI register classes.
