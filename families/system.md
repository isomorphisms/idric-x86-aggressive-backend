# System and privileged instructions

A public architecture may define system instructions that ordinary Apple applications cannot execute. Backend legality must distinguish `the ISA defines this mnemonic` from `user-mode Apple code may emit it`.
